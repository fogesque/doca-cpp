doca-cpp provides CMake targets:

    find_package(doca-cpp CONFIG REQUIRED)
    target_link_libraries(main PRIVATE doca-cpp::doca-cpp)

For direct header inclusion:

    #include <doca-cpp/core/device.hpp>